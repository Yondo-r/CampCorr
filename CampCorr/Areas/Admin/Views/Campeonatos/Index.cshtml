@model CampCorr.ViewModels.CampeonatoViewModel

@{
    ViewData["Title"] = "Editar";
}

<h1>Pagina de administração de campeonatos</h1>

<h4>@Model.NomeCampeonato</h4>

<hr />

<div class="form-outline form-white mb-4">
    <form asp-action="Edit">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="IdCampeonato" />
        <div class="form-group">
            <label asp-for="NomeCampeonato" class="control-label"></label>
            <input asp-for="NomeCampeonato" class="form-control" />
            <span asp-validation-for="NomeCampeonato" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Logo" class="control-label"></label>
            <input asp-for="Logo" class="form-control" />
            <span asp-validation-for="Logo" class="text-danger"></span>
        </div>
        
        <hr />
        <div class="form-group">
            <input type="submit" value="Salvar" class="btn btn-primary" />
        </div>
        <hr />
        @*@if (true)
        {
            <div>
                Editar temporada
            </div>
            @foreach (var item in Model.Temporada)
            {
                <div>
                    <a asp-action="Edit" asp-controller="Temporadas" asp-route-temporadaId="@item.TemporadaId">
                        @item.AnoTemporada.ToString()
                    </a>
                </div>
            }
        }
        else
        {
            <div>
                Adicionar temporada
            </div>
        }*@

            
    </form>
</div>


<div>
    <a asp-action="Index">Back to List</a>
</div>